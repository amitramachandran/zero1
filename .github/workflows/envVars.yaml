name: Show GitHub Environment Variables

on:
  push:  
jobs:
  show-env:
    runs-on: ubuntu-latest
    steps:
      - name: Print GitHub environment variables
        run: |
          echo "GITHUB_REPOSITORY: $GITHUB_REPOSITORY"
          echo "GITHUB_REF: $GITHUB_REF"
          echo "GITHUB_SHA: $GITHUB_SHA"
          echo "GITHUB_ACTOR: $GITHUB_ACTOR"
          echo "GITHUB_EVENT_NAME: $GITHUB_EVENT_NAME"
          echo "GITHUB_WORKFLOW: $GITHUB_WORKFLOW"
          echo "GITHUB_RUN_ID: $GITHUB_RUN_ID"
          echo "GITHUB_RUN_NUMBER: $GITHUB_RUN_NUMBER"
          echo "GITHUB_JOB: $GITHUB_JOB"
          echo "GITHUB_ACTION: $GITHUB_ACTION"
          echo "GITHUB_ACTIONS: $GITHUB_ACTIONS"
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          echo "GITHUB_HEAD_REF: $GITHUB_HEAD_REF"
          echo "GITHUB_BASE_REF: $GITHUB_BASE_REF"

      - name: Show event JSON path
        run: |
          echo "Event file path: $GITHUB_EVENT_PATH"

      - name: Check if event JSON exists
        run: |
          if [ -f "$GITHUB_EVENT_PATH" ]; then
            echo "✅ Event JSON file exists."
            cat "$GITHUB_EVENT_PATH"
          else
            echo "❌ Event JSON file does not exist."
          fi
